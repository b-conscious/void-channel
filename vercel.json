{
  "buildCommand": "cd mobile && npm install && npx expo export --platform web && node inject-og.js",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
