# ROADMAP 2026-06-12: from here to the outcome in the plan

Self-contained. Anchored to THE_VOID_BUILD_PLAN.md (strategy, JOBs 0-21, four modes),
BUILD_PLAN.md (ops log), the handoffs, and this session's debug run (slices 1-56). Flat
register, no em dashes, sentence case. This sequences; the plan specifies.

## The one honest truth that shapes everything
WATCH (Void Channel) is the only built mode, and the LIVE site (voidtv.net) is still the OG
build. Everything from the upgrade run, slices 1 through 56, era-lean, snacks, kids channels,
the catalog, the playability vet, the debug fixes, is LOCAL. Prod is untouched by B's standing
ruling (OG stays live until the upgrade is clean). So the gap between "what was discussed" and
"what a user sees today" is not missing features. It is that the clean, upgraded build has not
shipped. The whole session has been making the upgrade clean. It now is.

The risk to the outcome is not capability. It is a beautiful local build that never ships
because one determination (the Spine's production home) stays parked. That determination is
the gate. It is B's to make. Nothing below matters until it is made.

UPDATE 2026-06-12: B LIFTED the OG-stays-live freeze ("remove the og directive"). The upgrade
is cleared to ship. The cutover is no longer blocked by the freeze. It is now blocked only by
the Spine production-home determination and B's standing push-on-his-word rule. Move 1 is the
live next work.

## Definition of "running correctly" (acceptance, tied to the rulings)
1. The wall leads with the generational, recency-first texture (post slice-44), no nonsense,
   no future-dated spam, no foreign bleed, void TVs at 10% on staggers.
2. Search is live and freshens (P4); kids is fail-closed with the network channels (P1);
   snacks stay in the corral (P2); Most Popular is real (P3).
3. A played video resolves fast, vetted, with the show / more-like rails.
4. The above is TRUE IN PRODUCTION, not just localhost. This is the missing 4th.
5. The backend degrades visibly, not silently (P8 half done), and survives a deploy without a
   full resync (the Spine production-home determination).

## Where we are (state, honest)
- WATCH local build: STABLE. The original punch list (P1 to P4) is closed and verified; P8
  backend half done. The tree is locked at 184 files, runtime state out of git and the lock.
- Catalog: 34 verified series, ~3,970 films, fuzzy-fit live.
- Kids: 10 network channels, hard fail-closed gate, vouched inbox workflow.
- NOT shipped to prod. NOT cut over. Three other modes (LISTEN, PLAY, READ) are plan-only.

## The critical path (do in this order, do not skip)

### PHASE 0 — finish the stabilization (me, ~1 short session)
Close the debug session cleanly so the cutover ships something trustworthy.
- P5: re-measure kids cold-start now that P1's limiter + full-build cache landed. Confirm
  acceptable; if not, boot-warm the channels.
- P6 / P7: the dev-only console warnings and the videoUrlHQ cosmetic. Cheap, optional.
- One full pre-cutover verification pass on the wall, search, a played video, kids, snacks.
- Outcome: a known-clean local build with a green checklist. THEN Phase 1 is safe.

### PHASE 1 — MAKE IT REAL (B's Move 1; the gate; needs B's determinations)
This is the highest-value work in the whole project and it is mostly B's calls plus my
execution. Nothing in Phase 2+ should start before this.
- DETERMINATION (B): the Spine production home. Options from the marching orders: Render
  persistent disk, a real box, or Supabase Storage snapshot restore. Ephemeral disk means a
  full resync every deploy, which defeats the Spine. I will lay out the three with cost and
  failure modes when B wants; B picks.
- DETERMINATION (B): run the pending SQL in Supabase (schema-item-type.sql, and when the
  commons starts, schema-mature-pin.sql + schema-rls-audit.sql). Additive, zero downtime.
- Me: set SPINE_URL on Render, manual deploy, verify /health uptime resets (auto-deploy often
  does not cut over), confirm wall speed, gen lean, snacks, search, a played video LIVE.
- Me: Vercel frontend deploy; the JOB_19 version handshake already kills the stale-bundle
  phantom for users.
- Outcome: the upgraded build is the live product. Belief unblocked. This is the moment the
  "outcome discussed" becomes real for an actual visitor.

### PHASE 2 — THE VOICE LOOP (B's Move 2; identity + retention; mostly shipped)
Most of this already shipped locally during the upgrade run; Phase 1 carries it to prod.
- Confirm live: the pinned theme crate (JOB_14), PWA install + push + version handshake
  (JOB_19), intro-with-sound on desktop, the void-TV field.
- Remaining: web push is the deferred half of JOB_19; the aesthetic remodel (B's neon CRT
  icon system, the 16 sheets) is queued and needs B's direction on scope.

### PHASE 3 — THE COMMONS MVP (B's Move 3; the core mechanic)
The Curation Commons is the thread through the whole plan (section 0), not a feature. This is
what makes VOID different from Tubi: users curate alongside B, B's voice always leads.
- Prerequisite (B): the Supabase RLS audit + per-IP rate limiting. BUILT this session as
  security batch; B runs the SQL. These are blockers, not chores.
- JOB_18 Edit Layer: user grouping/description/tag corrections, trust tiers, materialize
  served values on write. This is also where B's "this item IS part of show X, it goes here"
  ruling lands (grouping already records source/confidence; human verdicts slot in at 1.0).
- JOB_15 Signal Strength: ratings as Bayesian-weighted signal feeding the composite rank.
- Voice primacy enforced throughout: B's entries are voice:b, community is signed and
  subordinate.

### PHASE 4 — THE OTHER THREE MODES (the expansion; one mode at a time)
Only after WATCH is live, looped, and curated. Each is a JOB lane already specified.
- LISTEN (Void Radio): JOB_3 audio mapper (the hard one), JOB_4 playback, JOB_5 library.
- PLAY (Void Arcade): JOB_6 game mapper + emulator surface.
- READ (Void Press): JOB_7 text mapper + reader.
- Then JOB_11 Unification (the four modes under one shell), JOB_12 social layer.

### STANDING PARALLEL LANE (runs alongside, worktree agent, B approved)
JOB_13 source adapters. NASA + Wikimedia Commons live; Wikidata is the index. Multi-source is
the existential hedge against IA concentration, not just content breadth. New sources drop in
without touching the Voice Loop.

## What is blocked on B vs what I can do
- B only (determinations, talk-before-deciding): Spine production home; run the Supabase SQL;
  founding-member $5/year vs monthly; TMDB pursue vs drop (recommend DROP, licence wall);
  aesthetic remodel direction; which crates stay in kids; theme cadence and first copy.
- Me (execution, on B's go): everything else. Phase 0 now. Phase 1 the moment B picks the
  Spine home. The client "reconnecting" banner (P8 user half). The show page (catalog).

## The funding reality (watchlist, must not be ignored)
Free-tier cliffs (Render, Vercel, Supabase, Upstash) convert a traffic spike into outage or a
bill. The funding circuit (JOB_9 mechanics, donation attribution) should be live BEFORE any
deliberate growth push. Do not promote the live site hard until the circuit and the cost
posture are set. This is a Phase 1.5 item, between cutover and any launch push.

## Immediate next 3 steps (concrete)
1. Me: Phase 0 close-out (P5 re-measure, optional P6/P7, full verification pass).
2. B: decide the Spine production home so Phase 1 can run. I will present the 3 options on
   request with cost and failure modes.
3. B: the OG-stays-live freeze is LIFTED (done 2026-06-12). What remains for the cutover is
   the Spine-home call (step 2) and B's explicit go to push. Then I execute Phase 1.
